#include "main.h"
/**
 * _islower - check for lower case alphabet
 * c - is assigned to check the balance
 * Return: 1 (success) 0 (false)
 */

int _islower(int c)
{
	if (c >= 97 && c <= 122)
	{
		return (1);
	}
		return (0);
}
